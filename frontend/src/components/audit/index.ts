/**
 * Phase 5 audit component: the compact integrity read-out for the audit chain.
 * The ledger's other routes exist, but the UI deliberately shows only a verdict.
 */
export { LedgerIntegrity } from './LedgerIntegrity';
