"""Domain models.

Phase 1 serves plain dict records validated at the API boundary by the Pydantic
schemas in ``app.schemas``. ORM models (SQLAlchemy) arrive in Phase 1.x/later
phases per docs/architecture.md §K. Intentionally empty for now.
"""
