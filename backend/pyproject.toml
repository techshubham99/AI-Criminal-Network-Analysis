[project]
name = "cna-backend"
version = "0.1.0"
description = "AI-Powered Criminal Network Analysis System — Backend API (SIH 2026, PS 26189)"
requires-python = ">=3.12,<3.13"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-ra -q"
filterwarnings = ["ignore::DeprecationWarning"]
